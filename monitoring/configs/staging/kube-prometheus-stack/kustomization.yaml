apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - grafana-tls-secret.yaml


commonLabels:
  app.kubernetes.io/name: grafana
  environment: staging
  managed-by: fluxcd
  layer: monitoring-config
